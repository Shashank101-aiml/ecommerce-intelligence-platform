# Part 2 (MLOps extension) — not implemented yet.
#
# Planned: build the churn-model feature set on top of warehouse.fact_sales /
# fact_customer_retention (recency, frequency, monetary, tenure, category
# mix, etc.), using a chronological split so training never sees
# future-dated behavior relative to the churn label's inactivity window.
